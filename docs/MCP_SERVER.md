로컬 MCP 서버는 설정에서 활성화하면 `http://127.0.0.1:8765/mcp` 로 JSON-RPC 요청을 받습니다.
지원 도구는 `ari_tts`, `ari_notify`, `ari_open_app`, `ari_take_screenshot`, `ari_get_system_info` 입니다.
포트는 설정의 `mcp_server_port` 값으로 변경할 수 있습니다.
