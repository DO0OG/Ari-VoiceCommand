# 패키지 구조 고도화 (2026-03-26):
#   - NVIDIA NIM LLM 제공자 추가 (OpenAI-호환 API, nvapi- 키)
#   - 시스템 명령 시간 파싱 개선 (N분 뒤/HH시에 컴퓨터 꺼줘 → shutdown /s /t <초>)
#   - AI 자율성 강화 (복합 키워드 확장, escalation 조건 보강)
#   - CosyVoice3 로컬 TTS 추가 최적화 (cudnn.benchmark, 동적 ODE steps 3/5)
#   - AppState 패턴으로 전역 변수 통합, ConfigManager RLock 교착 해결
#   - 루트 wrapper 파일 제거 및 패키지 경로 일괄 정리
#   - 로그 파일 자동 순환 (최대 10개 보관)
#   - CommandRegistry 우선순위 기반 정렬
# 이전 업데이트 (2026-03-25): 자율 실행 고도화, 파일 작업군 확대,
#   GUI/브라우저 자동화 강화, 플러그인 로더, 테마 시스템, TTS 안정화
# 개발 검증: validate_repo.py + unittest discover

# ── 음성 인식 ──────────────────────────────────────────────────────────────────
SpeechRecognition==3.10.1
pyaudio==0.2.14

# ── GUI ────────────────────────────────────────────────────────────────────────
PySide6==6.6.1
pyautogui>=0.9.54
pygetwindow>=0.0.9
pyperclip>=1.8.2
opencv-python-headless>=4.9.0.80

# ── AI / LLM ───────────────────────────────────────────────────────────────────
# OpenAI SDK: OpenAI, Groq, Mistral, Gemini, OpenRouter, NVIDIA NIM 공용
groq>=0.11.0
openai>=1.0.0
anthropic>=0.25.0

# ── TTS / 오디오 ───────────────────────────────────────────────────────────────
fish-audio-sdk>=0.1.0
numpy>=1.26.0
pydub==0.25.1
websockets==12.0
# edge-tts>=6.1.9
# elevenlabs>=1.0.0
# torch>=2.0.0
# torchaudio

# ── 네트워크 / 브라우저 자동화 ─────────────────────────────────────────────────
certifi>=2024.2.2
duckduckgo-search>=6.1.0
httpx>=0.27.0
requests>=2.32.0
selenium>=4.18.0
webdriver-manager>=4.0.1

# ── 시스템 (Windows) ───────────────────────────────────────────────────────────
comtypes>=1.4.1
psutil>=5.9.0
pycaw>=20230407

# ── 빌드 및 유틸리티 ───────────────────────────────────────────────────────────
huggingface_hub>=0.22.0
nuitka>=2.1.0
python-dotenv>=1.0.0
reportlab>=4.1.0  # auto/pdf 문서 저장
watchdog>=4.0.0
